---
const year = new Date().getFullYear();

const navLinks = [
  { title: "Services", href: "/services" },
  { title: "How We Work", href: "/how-we-work" },
  { title: "Results", href: "/results" },
  { title: "About", href: "/about" },
  { title: "Insights", href: "/insights" },
];

const socialLinks = [
  { name: "X (Twitter)", href: "#", icon: "X" },
  { name: "LinkedIn", href: "#", icon: "in" },
  { name: "Spotify", href: "#", icon: "sp" },
];
---

<footer class="bg-sky-dark border-t border-sky-card py-12 lg:py-20">
  <div class="container-custom grid grid-cols-1 md:grid-cols-4 gap-12">
    <!-- Brand Column -->
    <div class="col-span-1 md:col-span-2 space-y-6">
      <a href="/" class="text-3xl font-bold tracking-tighter text-white">
        SkyAbove<span class="text-sky-blue">.</span>
      </a>
      <p class="text-sky-muted text-lg max-w-sm">
        We build the experimentation + automation engine that increases output and learning speed while tightening measurement and reducing waste.
      </p>
      
      <!-- Social Links -->
      <div class="flex items-center space-x-6">
        {socialLinks.map((social) => (
          <a href={social.href} class="text-sky-muted hover:text-white transition-colors" aria-label={social.name}>
            <!-- Simple text icons for now, replace with SVGs later -->
            <span class="font-mono font-bold text-xl">{social.icon}</span>
          </a>
        ))}
      </div>
    </div>

    <!-- Navigation Column -->
    <div class="col-span-1 space-y-6">
      <h4 class="text-sm font-bold uppercase tracking-widest text-sky-amber">Explore</h4>
      <ul class="space-y-4">
        {navLinks.map((link) => (
          <li>
            <a href={link.href} class="text-sky-muted hover:text-white transition-colors">
              {link.title}
            </a>
          </li>
        ))}
        <li>
            <a href="/contact" class="text-sky-blue hover:text-white transition-colors font-bold">
              Book a Strategy Call
            </a>
        </li>
      </ul>
    </div>

    <!-- Legal/Reference Column -->
    <div class="col-span-1 space-y-6">
      <h4 class="text-sm font-bold uppercase tracking-widest text-sky-amber">Reference</h4>
      <ul class="space-y-4">
        <li>
          <a href="https://befoundonai.com" target="_blank" rel="noopener noreferrer" class="text-sky-muted hover:text-white transition-colors">
            Be Found on AI Book
          </a>
        </li>
        <li>
          <a href="/privacy" class="text-sky-muted hover:text-white transition-colors">
            Privacy Policy
          </a>
        </li>
        <li>
          <a href="/terms" class="text-sky-muted hover:text-white transition-colors">
            Terms of Service
          </a>
        </li>
      </ul>
    </div>
  </div>

  <div class="container-custom mt-12 pt-8 border-t border-sky-card/50 flex flex-col md:flex-row justify-between items-center text-sm text-sky-muted">
    <p>&copy; {year} SkyAbove. All rights reserved.</p>
    <p class="mt-4 md:mt-0">Engineered in Bastrop, TX.</p>
  </div>
</footer>
