---
interface Props {
    title: string;
    description: string;
    deliverables: string[];
    icon: string;
    reversed?: boolean;
}

const {
    title,
    description,
    deliverables,
    icon,
    reversed = false,
} = Astro.props;
---

<section
    class="section-padding border-b border-sky-card/30 relative overflow-hidden"
>
    <div
        class="container-custom grid grid-cols-1 lg:grid-cols-2 gap-16 items-center"
    >
        <!-- Text Content -->
        <div class={`space-y-8 ${reversed ? "lg:order-2" : ""}`}>
            <div
                class="w-16 h-16 bg-sky-blue/10 rounded-xl flex items-center justify-center text-3xl text-sky-blue border border-sky-blue/20"
            >
                {icon}
            </div>

            <h2 class="text-3xl md:text-4xl font-bold">{title}</h2>
            <p class="text-xl text-sky-muted leading-relaxed">
                {description}
            </p>

            <div class="space-y-4 pt-4">
                <h3
                    class="text-sm font-bold uppercase tracking-wider text-sky-amber"
                >
                    Deliverables
                </h3>
                <ul class="grid grid-cols-1 md:grid-cols-2 gap-3">
                    {
                        deliverables.map((item) => (
                            <li class="flex items-start space-x-3 text-sky-muted">
                                <span class="text-sky-blue mt-1.5 text-xs">
                                    ●
                                </span>
                                <span>{item}</span>
                            </li>
                        ))
                    }
                </ul>
            </div>
        </div>

        <!-- Visual / Graphic placeholder -->
        <div
            class={`relative h-[400px] bg-sky-card/30 rounded-2xl border border-sky-card overflow-hidden group ${reversed ? "lg:order-1" : ""}`}
        >
            <div
                class="absolute inset-0 bg-gradient-to-br from-sky-blue/5 to-transparent"
            >
            </div>
            <div
                class="absolute inset-0 flex items-center justify-center opacity-30 group-hover:opacity-50 transition-opacity"
            >
                <!-- Placeholder pattern -->
                <div
                    class="w-3/4 h-3/4 border-2 border-dashed border-sky-muted/20 rounded-xl"
                >
                </div>
            </div>
        </div>
    </div>
</section>
